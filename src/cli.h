#pragma once

// Serial console for testing without full hardware: type `help` in the
// monitor. Lets you simulate sensors, drive the alarm and relays, etc.
namespace cli {
void loop();
}  // namespace cli
